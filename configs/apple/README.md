# Apple Silicon Config

Apple Silicon (M1/M2/M3/M4) support is planned for v0.2.

MLX-based inference will replace Ollama's CUDA backend for Apple GPUs.
Configuration files for MLX model variants will live here.
