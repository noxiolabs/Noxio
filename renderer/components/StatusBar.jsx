/**
 * @file StatusBar.jsx
 * @description Bottom status bar showing: VRAM usage meter, service health dots,
 * currently loaded model name, and cloud budget remaining (if cloud is enabled).
 * All data comes from Redux — no direct IPC in this component.
 *
 * TODO Phase 4: implement full StatusBar UI.
 */

import React from 'react';

/**
 * Application status bar.
 */
export default function StatusBar() {
  // TODO Phase 4: implement with VRAM meter, health dots, model name, budget
  return null;
}
