/**
 * @file Sidebar.jsx
 * @description Mode navigation sidebar. Lets the user switch between Chat,
 * Create, Voice, and Agent panels. Also shows a Gaming Mode button that
 * pauses all services and frees VRAM.
 *
 * TODO Phase 4: implement full navigation with active state and icon set.
 */

import React from 'react';

/**
 * Sidebar navigation component.
 * @param {Object} props
 * @param {string} props.activeMode - Current active mode
 * @param {Function} props.onModeChange - Called with the new mode string
 */
export default function Sidebar({ activeMode, onModeChange }) {
  // TODO Phase 4: implement
  return null;
}
