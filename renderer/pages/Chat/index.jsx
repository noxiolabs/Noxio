/**
 * @file Chat/index.jsx
 * @description Chat panel: streaming conversation with local LLMs or cloud fallback.
 * Uses @chatscope/chat-ui-kit-react for the message list and input.
 *
 * TODO Phase 4: implement full chat panel with streaming, model selector,
 * conversation history sidebar, and markdown rendering.
 */

import React from 'react';

export default function ChatPanel() {
  // TODO Phase 4
  return (
    <div className="flex items-center justify-center h-full text-zinc-600 text-sm">
      Chat panel — Phase 4
    </div>
  );
}
