/**
 * @file Create/index.jsx
 * @description Create panel: image generation via ComfyUI. Exposes a simple
 * prompt input, style presets, quality slider, and output gallery. The ComfyUI
 * node graph is never exposed to the user.
 *
 * TODO Phase 5: implement.
 */

import React from 'react';

export default function CreatePanel() {
  // TODO Phase 5
  return (
    <div className="flex items-center justify-center h-full text-zinc-600 text-sm">
      Create panel — Phase 5
    </div>
  );
}
